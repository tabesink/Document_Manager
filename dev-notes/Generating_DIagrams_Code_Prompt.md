Generate a clean, flowchart-style digital diagram based on the following code. Visually represent the state structure, grouping related fields and functions into labeled sections. Include state properties (booleans, numbers, strings, arrays, etc.), setter/update functions, and any computed or derived logic. Highlight logical groupings and data flow between them. Use modern, minimal visuals suitable for a developer audience—clear labels, directional arrows where needed, and icons if helpful. Ensure the layout is structured and easy to read.